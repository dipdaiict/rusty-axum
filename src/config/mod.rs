// Main module for the `config` folder.
// Re-export configuration submodules like `database` and `settings`.
// Initialize configuration during application startup.

pub mod database;
pub mod settings;