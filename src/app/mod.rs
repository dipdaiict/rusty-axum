// Main module for the `app` folder.
// Re-exports other submodules like `routes`, `handlers`, `state`, etc.
// Add common imports and glue code related to the app's functionality here.


// Example: [Re-export submodules]
pub mod state; 
pub mod errors;
pub mod middlewares;